CURDATE()
NOW()
DATE()
DATE_ADD()
DATE_SUB()
DATEDIFF()
TIMESTAMPDIFF()
DATE_FORMAT()
STR_TO_DATE()

SELECT STR_TO_DATE('2026-07-14 15:30:00', '%Y-%m-%d %H:%i:%s') AS ts;

DATE_FORMAT(InvoiceDate, '%Y-%m')

DATE_SUB(CURDATE(), INTERVAL 30 DAY)

DATEDIFF(CURDATE(), DATE(InvoiceDate)) AS days_since_invoice

CURDATE() AS current_date_utc
NOW() AS current_datetime_utc
UTC_TIMESTAMP()

```sql
-- 这两个跟着服务器时区走
CURDATE()
NOW()

-- 这两个永远是 UTC，不管服务器怎么配置
UTC_DATE()
UTC_TIMESTAMP() AS current_datetime_utc
```

CONVERT_TZ(created_at, '+00:00', 'America/New_York') AS created_at_ny  -- 如果返回 NULL → 说明时区表没装
CONVERT_TZ(created_at, '+00:00', '-05:00') AS created_at_est

TIMESTAMPDIFF(HOUR, created_at, UTC_TIMESTAMP()) AS hours_since_created

