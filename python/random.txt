import random

# 常见抽样
random.choice(base)                 # 抽1个
random.sample(base, n)             # 抽n个, 不重复
random.choices(base, k=n)          # 抽n个, 可重复
random.shuffle(base)               # 打乱顺序, 原地修改

# 数值抽样
random.randint(a, b)               # [a, b]整数
random.randrange(start, stop, step)# 按步长抽整数
random.uniform(a, b)               # [a, b]浮点数
random.random()                    # [0, 1)浮点数 可能是 0.374829153...

# 常用列表生成
[random.random() for _ in range(n)]  # 生成 0~1 小数列表
[random.randint(0, 1) for _ in range(n)]  # 生成只含 0/1 的列表
random.sample(range(start, stop), n)

# 带权重
random.choices(base, weights=w, k=n)

# 可复现
random.seed(42)
